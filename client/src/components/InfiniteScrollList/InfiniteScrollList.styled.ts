import styled from "styled-components";

export const StyledInfiniteScrollList = styled.div`
	position: relative;
`;

export const ListLastElement = styled.div`
	position: absolute;
	bottom: 0;
	left: 0;
	width: 100%;
`;

export const ListLoaderWrapper = styled.div`
	position: relative;
	margin: 0 auto;
`;
