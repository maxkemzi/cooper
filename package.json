{
	"name": "cooper",
	"version": "1.0.0",
	"scripts": {
		"build-client": "npm run build --prefix client",
		"postinstall": "npm i --prefix server",
		"heroku-postbuild": "npm i --prefix client && npm run build-client"
	}
}
