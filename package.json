{
	"name": "cooper",
	"version": "1.0.0",
	"scripts": {
    "build-client": "cd ./client && npm run build",
		"install-client": "cd ./client && npm i",
		"postinstall": "npm start --prefix server",
		"postbuild": "npm run install-client && npm run build-client"
	}
}
